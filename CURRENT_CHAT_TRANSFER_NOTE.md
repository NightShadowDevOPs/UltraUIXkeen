# UI Mihomo / Ultra — перенос в новый чат

## Текущее состояние
- Текущая версия UI: **v1.2.129**
- Router-agent: **0.6.26**
- Основной репозиторий: `NightShadowDevOPs/UltraUIXkeen`
- Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
- Путь на роутере: `/opt/UltraUIXkeen`

## Что сделано в v1.2.129
- исправлен runtime-сбой рендера backup workspace в `src/components/router/AgentCard.vue`: inspection panel больше не обращается к `item` вне области `v-for`
- добавлены template refs для maintenance workspace, чтобы lazy-load и автопрокрутка в `Router → Резервные копии` работали предсказуемее
- router-agent в этом релизе не менялся и остаётся `0.6.26`

## Что проверить после выкладки
- `Router → Резервные копии → Загрузить` теперь реально поднимает backup workspace, а не оставляет пустой экран
- `Проверить` раскрывает проверку архива внутри списка, не ломая весь блок
- `Сделать бэкап` и остальные действия остаются рабочими

## Следующий релиз
- **v1.2.130** — Router network workspace hardening: дочистить сетевой рабочий экран и убрать остаточную перегруженность
