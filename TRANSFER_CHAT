# UI Mihomo / Ultra — transfer note for next chat

Дата: **2026-04-20**
Текущая версия UI: **v1.2.157**
Текущая версия router-agent: **0.6.32**
Репозиторий: `NightShadowDevOPs/UltraUIXkeen`
Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
Путь на роутере: `/opt/UltraUIXkeen`

## Что вошло в v1.2.157
- В `Задачи → Прокси-провайдеры → панели управления` добавлена очистка отключённых/осиротевших записей.
- Строки, которые живут только в сохранённых UI-настройках и уже отсутствуют в активном списке провайдеров, помечаются бейджем **«сохранён»**.
- Можно удалить одну строку или сразу все такие записи кнопкой **«Очистить отключённые»**.
- При удалении чистятся связанные UI-настройки: URL панели, индивидуальный SSL-порог, иконка провайдера и локальный кэш даты SSL по панели.
- `router-agent` оставлен на `0.6.32`; live traffic / SSL runtime / provider checks не менялись.

## Что важно помнить дальше
- нельзя ломать автоматическую проверку SSL-сертификатов провайдеров
- обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`
- если меняется router-agent, нужно синхронизировать версию в `router-agent/install.sh`, status API, docs и HA handoff bundle
- любые следующие доработки раздела Трафик делать осторожно: сначала runtime-безопасность, потом украшательства

## Что проверить после обновления
1. В `Задачи → Прокси-провайдеры → панели управления` saved-only строки удаляются по одной и массово.
2. Для активных провайдеров кнопка `Открыть` и SSL-индикаторы работают как раньше.
3. В Обзоре диаграмма весов трафика остаётся живой и работает как раньше.
4. Реальный трафик через роутер и QoS runtime не деградировали после обновления.

## Следующий логичный шаг
- после живой проверки `v1.2.157` можно продолжать safe upstream review и брать только те изменения, которые снижают burst/load или лишние UI-перерисовки без риска для runtime.
