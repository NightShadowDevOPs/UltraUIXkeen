# UI Mihomo / Ultra — transfer note for new chat

Дата: 2026-04-20
Текущая версия UI: **v1.2.150**
Текущая версия router-agent: **0.6.32**
Папка проекта: **Y:\Мой диск\Git\UltraUIXkeen**
Путь на роутере/сервере: **/opt/UltraUIXkeen**

## Что вошло в v1.2.150
- UI поднят до `1.2.150`
- router-agent оставлен на `0.6.32`; backend telemetry/API контракт не менялся
- в `HostQosCard` secondary summary/runtime polling стал реже, а live host telemetry осталась на отдельном более частом цикле только для реально открытого/нужного контекста
- в `UserTrafficStats` background refresh для QoS/runtime сделан спокойнее
- повторные `status`, `qos_status` и `lan_hosts` reads кратко кэшируются в QoS-виджетах, чтобы UI не дублировал одни и те же agent-запросы
- silent auto-refresh в Host QoS больше не мигает loading-состоянием на каждом таймере
- задача релиза: ещё уменьшить лишний polling и не задеть реальный traffic/runtime path на роутере

## Что важно помнить
- Для этого проекта пользователь всегда ждёт: **архив релиза**, **commit message отдельно**, **команды для роутера отдельно**, **обновлённые docs**.
- Обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`.
- Если меняется `router-agent`, нужно синхронизировать версию в `install.sh`, status API и docs. В этом релизе agent не менялся.
- Автоматическую проверку SSL-сертификатов провайдеров ломать нельзя.
- При работе с Трафиком приоритет такой: сначала runtime-безопасность, потом украшательства.

## Что проверено/не проверено
- Кодовая часть релиза собрана как архив и документация обновлена.
- Финальная ручная проверка нужна уже на роутере после установки: важно убедиться, что Overview weights chart живой и реальный трафик не просел.

## Что проверить после обновления
1. **Обзор → диаграмма весов трафика**: график живой и не падает в нули при кратком telemetry hiccup.
2. **Трафик → Устройства**: карточка QoS устройств обновляется спокойнее и без лишнего loading-мигания.
3. **Трафик → Пользователи**: профили/QoS подтягиваются спокойнее на фоне, без частой дёрготни.
4. Реальный трафик через роутер не деградировал после установки hotfix.

## Следующий логичный шаг
- Следующим логичным шагом после `v1.2.150` сделать `v1.2.151`: посмотреть безопасные cherry-pick идеи из upstream и при необходимости очень выборочно дорезать только действительно вторичный polling, не трогая основной live traffic contour.
