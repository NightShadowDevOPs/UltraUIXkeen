# UltraUIXkeen — перенос в новый чат

Проект: UltraUIXkeen (форк Zashboard UI)
Репозиторий: NightShadowDevOPs/UltraUIXkeen
Линейка версий: 1.2.x
Текущая версия архива: v1.2.17
router-agent: 0.6.8

Последний фикс в этом релизе:
- Host QoS переведён на общий shared store для applied/draft профилей, поэтому иконки и выбранные приоритеты больше не должны слетать после полного reload страницы
- список `Host QoS priority` теперь собирается шире: кроме `lan_hosts` и `host_traffic_live` он включает source IP из активных Mihomo connections и уже применённые QoS entries, поэтому в список попадает больше устройств из живого списка
- live device list теперь не затирает сохранённый QoS-кэш пустым/неполным `qos_status`, а мягко мержит router-side статус в локальный кэш
- router-agent в этом релизе не менялся и остаётся 0.6.8



Ключевые особенности:
- отдельные пункты меню: Прокси, Прокси-провайдеры и Подписки
- multi-cloud backup через rclone и router-agent
- cloud remotes из одного rclone.config через RCLONE_REMOTES
- cron backup на роутере работает через /opt/var/spool/cron/crontabs/root
- дополнительная диаграмма трафика в стиле Netcraze с разделением по цветам: общий WAN / Mihomo / VPN-туннели / bypass
- у новой диаграммы трафика разведены более контрастные цвета легенды, чтобы серии не сливались
- обнаруженные дополнительные VPN/туннели выводятся на диаграмме отдельными сериями по интерфейсам, а активные хосты по живым соединениям Mihomo показываются отдельным стабильным списком под диаграммой, чтобы строки не скакали
- в блоке роутера есть проверка обновления прошивки по официальной странице Netcraze и уведомление о новой версии
- для дополнительных туннелей усилено распознавание WireGuard/OpenVPN и похожих интерфейсов
- в карточке роутера есть модель, прошивка, kernel, arch, Mihomo, XKeen, температура, load average 1/5/15m, свободная RAM и storage
- активный раздел в левом меню подсвечен
- SSL-проверка не должна скрывать список провайдеров; ошибки SSL показываются отдельно, а сами данные по сертификатам снова заполняются в agent
- вкладка `Подписки` умеет отдавать клиентские агрегированные ссылки и QR-коды для Mihomo/Clash и V2Ray/Xray клиентов; сам клиент подключается напрямую к серверам провайдеров, а роутер только отдаёт подписку
- вкладка `Подписки` теперь умеет собирать те же ссылки через опубликованный HTTPS base URL, если router-agent опубликован за reverse proxy; сам сценарий публикации спрятан в расширенные настройки и не мешает локальному роутерному режиму

Что важно не ломать:
- рабочую логику активности провайдеров
- крестик у провайдера — только disconnect active sessions
- cloud/local restore backup
- поддержку RCLONE_CONFIG, RCLONE_REMOTES, RCLONE_PATH
- сценарий Published HTTPS base на вкладке `Подписки`: для LAN можно оставить поле пустым; при реальной публикации наружу пользовательские ссылки должны уметь уходить на опубликованный HTTPS endpoint

Отложенные задачи:
- довести QoS/приоритизацию хостов: добавить привязку к MAC, более тонкие профили и безопасную увязку с существующими лимитами канала
- при желании позже добавить явный router-agent `shape_status`, чтобы UI мог показывать уже активные жёсткие лимиты даже без предварительной синхронизации local user-limits с другого ПК
- разобраться с отображением архивов в облачном проводнике
- продолжить проверку и доводку подсчёта трафика Сегодня у прокси-провайдеров, в первую очередь на длинных сессиях и пограничных кейсах
- при желании позже показать в UI отдельный статус/ошибки SSL probe по недоступным провайдерам
- следующий большой шаг по подпискам: проверить реальный HTTPS endpoint снаружи и решить, нужен ли отдельный JSON schema/endpoint под V2RayTun/Xray поверх текущего groundwork; параллельно можно дополировать тексты и статусные подсказки на вкладке Подписки после живой проверки импорта в клиентах
- доделать Host QoS после переноса в `Пользователи → Трафик по пользователям`: привязка к MAC, более точная синхронизация списка устройств и безопасная увязка с уже существующими лимитами канала

Настройки backup:
- RCLONE_CONFIG="/opt/etc/rclone.config"
- RCLONE_REMOTES="gdrive_secure,yadisk_secure"
- RCLONE_PATH="backup/Zash"

Быстрая проверка multi-cloud backup:
- /opt/zash-agent/backup.sh "gdrive_secure,yadisk_secure"
- tail -n 200 /opt/zash-agent/var/backup.last.log
- cat /opt/zash-agent/var/backup.last.json

История последних релизов:
- UI v1.2.18 / agent 0.6.8: управление Host QoS перенесено в раздел `Пользователи → Трафик по пользователям`; на странице `Router` вместо полного QoS-блока оставлена компактная карточка-переход, чтобы QoS и трафик по устройствам жили в одном месте.
- UI v1.2.17 / agent 0.6.8: Host QoS переведён на общий shared store между QoS-карточкой и live device list; иконки/профили больше не должны слетать после reload страницы, а список `Host QoS priority` теперь включает source IP из активных Mihomo connections и уже применённые QoS entries, чтобы подтягивать больше устройств из живого списка.
- UI v1.2.16 / agent 0.6.8: Host QoS сохраняет выбранные и применённые профили при переходах по меню на том же ПК; live device list реально подмешивает `qos_status` в активные host rows и показывает заметные QoS-иконки/уровни прямо в строках устройств.
- UI v1.2.15 / agent 0.6.8: в списке устройств под живым трафиком и в карточке Host QoS priority добавлены компактные визуальные индикаторы уровней QoS; NetcrazeTrafficCard теперь подгружает `qos_status` от router-agent и показывает applied profile прямо в строке устройства, чтобы приоритет читался без перехода в отдельный блок QoS.
- UI v1.2.14 / agent 0.6.8: блок Host QoS priority сделан сворачиваемым по умолчанию; в QoS-списке имена хостов теперь берутся из присвоенных Source IP labels; общая users_db синхронизация через router-agent расширена и теперь включает user bandwidth limits, чтобы лимиты по каналам и хостам были видны на разных ПК, а не жили только в localStorage одного браузера.
- UI v1.2.11 / agent 0.6.6: проект переведён на новый основной GitHub-репозиторий `NightShadowDevOPs/UltraUIXkeen`; ссылки на rolling release, latest release API, install/update URL router-agent и GitHub-ссылки в UI/docs теперь указывают на новый аккаунт, чтобы обновления и релизы шли уже без хвостов `messireL/ZashUIFork`.
- UI v1.2.9 / agent 0.6.5: для V2RayTun на вкладке `Подписки` снова выведены отдельный `format=v2raytun` URL, deeplink `v2raytun://import/...` и QR-режим для живой LAN-проверки; сам `router-agent` теперь дублирует `profile-title`, `profile-update-interval` и `update-always` не только в HTTP headers, но и внутри body как `#...`-заголовки перед merged share-links, чтобы лучше совпасть с документированным body-header flow V2RayTun.
- UI v1.2.8 / agent 0.6.4: исправлена логика cloud upload в `backup.sh` / cron — если `RCLONE_REMOTE`/`RCLONE_REMOTES` указывают на удалённые или устаревшие remotes, agent теперь автоматически падает обратно на реальные remotes из `rclone.conf` и продолжает выгружать новые архивы, а не только показывать старые в UI.
- UI v1.2.6 / agent 0.6.2: исправлена генерация `format=mihomo` в `router-agent` — секция `proxy-groups:` теперь выводится явно перед группами `Manual / Auto / Failover / Balance`; это устраняет поломанный YAML, который мог приводить к неочевидным ошибкам или тайм-аутам при импорте в Clash-совместимые клиенты и Hiddify.
- UI v1.2.7 / agent 0.6.3: исправлена логика облачной истории/restore бэкапов: если `RCLONE_REMOTE`/`RCLONE_REMOTES` указывают на удалённые или устаревшие remotes, agent теперь автоматически падает обратно на реальные remotes из `rclone.conf` и не прячет облачные архивы только из-за битых имён.
- UI v1.2.5 / agent 0.6.1: на вкладке `Подписки` добавлен отдельный блок сценариев использования с явным разделением `Разовый импорт дома` vs `Обновляемая подписка`; сверху также показана готовность клиентов (Mihomo/Clash, v2rayNG/Hiddify, V2RayTun), а в карточках подписок появилось отдельное пояснение, что сохранённый локально после домашнего импорта конфиг может использоваться вне дома без доступа к роутеру, но автообновление по-прежнему требует пути к router-agent.
- UI v1.2.4 / agent 0.6.1: на вкладке `Подписки` добавлены явные статусные плашки `Сейчас используется` для Mihomo/Clash и V2Ray/Xray, чтобы было сразу видно, используется ли локальный router-agent URL или внешний HTTPS URL; кнопки копирования переименованы в `Копировать текущий URL` / `Копировать текущий JSON URL`, а русские подписи publication/JSON preview стали нормальными.
- UI v1.2.3 / agent 0.6.1: на вкладке `Подписки` локальный статусный бейдж переименован в `LAN-режим (локально)` и сделан более нейтральным, а кнопка раскрытия HTTPS-настроек переименована в более понятную `Настроить внешний HTTPS`.
- UI v1.2.0 / agent 0.6.0: старт блока HTTPS subscriptions groundwork; на вкладке `Подписки` убрана ложная готовность V2RayTun и добавлено честное предупреждение про необходимость нормального HTTPS endpoint, а в `router-agent` появился подготовительный `format=json` endpoint с JSON manifest (providers + merged share links) как каркас под будущий HTTPS / Xray-поток.
- UI v1.2.1 / agent 0.6.1: добавлен сценарий `Published HTTPS base` на вкладке `Подписки`; клиентские URL/QR/deeplink теперь можно строить от опубликованного HTTPS endpoint, а `format=json` научился возвращать канонические публичные ссылки через `X-Forwarded-*`.
- UI v1.2.2 / agent 0.6.1: сценарий `Published HTTPS base` убран из основного UX-потока и спрятан в расширенные настройки публикации; для обычного LAN-режима UI теперь честно показывает, что ничего дополнительно заполнять не нужно.

- v1.2.14 / agent 0.6.8: Host QoS expanded from 3 to 6 profiles; QoS list now shows applied profile badges and queue priority in-row.
