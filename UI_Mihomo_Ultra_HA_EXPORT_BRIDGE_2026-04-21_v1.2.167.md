# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-21**
Текущая версия UI: **v1.2.167**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.167` `router-agent` не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся только в UI-слое вокруг глобальной проверки свежести UI-сборки.
- Формат данных router-agent для HA, shape JSON и export bundle этим релизом не менялись.

## Что проверять после обновления
1. Лишняя автопроверка свежести UI-сборки не срабатывает при каждом обычном visible-resume.
2. Ручная проверка обновления UI работает штатно.
3. Hard refresh UI работает как раньше.
4. Router-agent / HA snapshot продолжает работать как раньше.
5. Overview: диаграмма весов трафика продолжает работать нормально, а реальный трафик через роутер не деградирует.

## Следующий мостовой шаг
- после подтверждения `v1.2.167` можно продолжать safe review только для низкорисковых operational/UI-хвостов без роста постоянного polling и без вмешательства в HA/export контракт
