# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-21**
Текущая версия UI: **v1.2.163**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.163` router-agent не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся только в UI-слое operational-карточек.
- Убраны дополнительные дублирующиеся wake-up refresh там, где компонент уже сам обновлялся через локальный `watch(...active...)`.
- Формат данных router-agent для HA, shape JSON и export bundle этим релизом не менялись.

## Что проверять после обновления
1. `Router -> System`, `Router -> Router agent`, `Router -> Host QoS`, `Traffic / Users` QoS stats не создают лишние дублирующиеся wake-up refresh.
2. Router-agent / HA snapshot продолжает работать как раньше.
3. `Задачи` логика `v1.2.162` продолжает работать нормально.
4. Overview: диаграмма весов трафика продолжает работать нормально.
5. Реальный трафик через роутер не деградирует.

## Следующий мостовой шаг
- после подтверждения `v1.2.163` можно продолжать safe cherry-pick review из upstream и дальше чистить тяжёлые UI-хвосты без роста постоянного polling и без вмешательства в HA/export контракт
