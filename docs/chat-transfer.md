# UI Mihomo / Ultra — transfer note for next chat

Дата: **2026-04-20**
Текущая версия UI: **v1.2.151**
Текущая версия router-agent: **0.6.32**
Репозиторий: `NightShadowDevOPs/UltraUIXkeen`
Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
Путь на роутере: `/opt/UltraUIXkeen`

## Что вошло в v1.2.151
- UI поднят до `1.2.151`
- router-agent оставлен на `0.6.32`, backend telemetry/runtime path не менялся
- в `HostQosCard` и `UserTrafficStats` второстепенный polling теперь ещё и привязан к видимости карточки: если блок не в viewport, фоновый refresh не крутится зря
- при возврате карточки в viewport выполняется мягкий refresh, чтобы пользователь не видел протухшие QoS/runtime данные
- основной live path для трафика и обзорная диаграмма весов в Обзоре специально не переводились в lazy/off-screen режим

## Что важно помнить дальше
- нельзя ломать автоматическую проверку SSL-сертификатов провайдеров
- обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`
- если меняется router-agent, нужно синхронизировать версию в `router-agent/install.sh`, status API, docs и HA handoff bundle
- любые следующие доработки раздела Трафик делать осторожно: сначала runtime-безопасность, потом украшательства

## Что проверить после обновления
1. В Обзоре диаграмма весов трафика остаётся живой как и раньше.
2. Если карточки QoS ниже экрана и не видны, лишний фоновый polling к роутеру не идёт.
3. После возврата к карточке данные быстро освежаются без ручного тыканья Refresh.
4. Реальный трафик через роутер и QoS runtime не деградировали после обновления.

## Следующий логичный шаг
- `v1.2.152`: после проверки на живом роутере посмотреть безопасные cherry-pick идеи из upstream и отдельно ревизовать ещё пару вторичных виджетов/сводок, которые можно подвязать к viewport без риска для UX.
