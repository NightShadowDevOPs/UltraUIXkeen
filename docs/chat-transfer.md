# UI Mihomo / Ultra — перенос в новый чат

## Текущее состояние
- Текущая версия UI: **v1.2.122**
- Router-agent: **0.6.26**
- Основной репозиторий: `NightShadowDevOPs/UltraUIXkeen`
- Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
- Путь на роутере: `/opt/UltraUIXkeen`

## Что сделано в v1.2.122
- исправлен build-break после v1.2.121: добавлен отсутствующий `src/components/router/ConnectionInfoCard.vue`
- `Router → Сеть` снова рендерится через существующие виджеты IP-проверки и latency
- обзорная переразбивка `Router → Обзор` из v1.2.121 сохранена
- авто-проверка SSL-сертификатов прокси-провайдеров не затрагивалась

## Что проверять после выкладки
1. CI/build проходит без ошибки `Could not load ... ConnectionInfoCard.vue`
2. `Router → Обзор` открывается как короткая сводка
3. `Router → Сеть` открывается и показывает оба блока: IP и latency/ping
4. `Router → Резервные копии` и `Traffic` не сломались после hotfix

## Следующий зафиксированный шаг
- **v1.2.123** — полировка `Router → Резервные копии`: явные действия по строкам архивов, меньше пустоты, лучше визуальная иерархия
