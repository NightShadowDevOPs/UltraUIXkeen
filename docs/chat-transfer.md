# UI Mihomo / Ultra — transfer note for new chat

Дата: 2026-04-19
Текущая версия UI: **v1.2.147**
Текущая версия router-agent: **0.6.31**
Папка проекта: **Y:\Мой диск\Git\UltraUIXkeen**
Путь на роутере/сервере: **/opt/UltraUIXkeen**

## Что вошло в v1.2.147
- поднят UI до `1.2.147`
- исправлен сломанный переход **Роутер → Трафик → Открыть**: теперь кнопка ведёт в реальный раздел **Трафик**, а не в протухшее имя маршрута
- переход из карточки **Трафик** на обзоре роутера теперь открывает основной traffic-workspace в контексте устройств/QoS
- на карточке роутера убран английский бейдж `devices / users / QoS`; вместо него теперь русская подпись
- в пользовательских подписях ключевой QoS-контур переименован из **Host QoS / хосты** в более понятные **QoS устройств / устройства**
- контракт данных для **Home Assistant не менялся**: структура payload/export оставлена прежней
- обновлены transfer-файлы, snapshot памяти модели и правила workflow

## Что важно помнить
- Для этого проекта пользователь всегда ждёт: **архив релиза**, **commit message отдельно**, **команды для роутера отдельно**, **обновлённые docs**.
- В docs нужно поддерживать файл переноса в новый чат и фиксировать не только код, но и рабочие правила/формат ответов.
- Если меняется `router-agent`, нужно синхронизировать версию в `install.sh` и status API. В этом релизе agent не менялся.
- При работе с HA-интеграцией нельзя самовольно менять структуру экспортируемых данных без отдельного запроса.

## Что проверено/не проверено
- Полную `npm run build` в контейнере прогнать не удалось: зависимости не установлены в текущем окружении.
- Поэтому после выкладки на роутер особенно важно вручную проверить навигацию из карточки **Роутер → Трафик** и видимые подписи QoS.

## Что проверить после обновления
1. **Роутер → Обзор → Трафик → Открыть**: кнопка должна реально переводить в левый раздел **Трафик**, без пустого клика.
2. После перехода должен открываться основной traffic-workspace с видимым блоком устройств/QoS, а не оставаться экран роутера.
3. На карточке **Трафик** и в связанных QoS-подписях не должно оставаться английского `Host` в пользовательском интерфейсе этого контура.
4. Экспорт в HA и существующие метрики должны остаться без изменений.

## Следующий логичный шаг
- Следующим логичным шагом после `v1.2.147` сделать `v1.2.148`: подчистить перегруженность раздела **Трафик**, сделать QoS/shaping визуально прозрачнее и не ломать bridge-контур HA.
