# UI Mihomo / Ultra — перенос в новый чат

## Текущее состояние
- Текущая версия UI: **v1.2.124**
- Router-agent: **0.6.26**
- Основной репозиторий: `NightShadowDevOPs/UltraUIXkeen`
- Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
- Путь на роутере: `/opt/UltraUIXkeen`

## Что сделано в v1.2.124
- верхняя плашка заголовка теперь показывает нормальные локализованные названия рабочих зон вместо сырого ключа вроде `routerTraffic`
- для `Router` добавлены явные заголовки `Обзор / Резервные копии / Трафик / Сеть`
- для `Traffic` добавлены явные заголовки `Устройства / Пользователи`
- авто-проверка SSL-сертификатов прокси-провайдеров не затрагивалась

## Что проверять после выкладки
1. В верхней плашке и заголовке рабочей зоны больше не появляется сырой ключ `routerTraffic`
2. `Router → Трафик`, `Router → Резервные копии` и `Router → Сеть` показывают нормальные русские заголовки
3. `Traffic → Devices` и `Traffic → Users` тоже показывают нормальные локализованные названия
4. CI/build проходит и роутерские рабочие зоны открываются без регрессии

## Следующий зафиксированный шаг
- **v1.2.125** — полировка `Router → Резервные копии`: явные действия по строкам архивов, меньше пустоты, лучше визуальная иерархия
