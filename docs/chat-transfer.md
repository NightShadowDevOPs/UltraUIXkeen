UI Mihomo / Ultra — обновление переноса v1.2.80

Что изменилось в этом релизе:
- в `Mihomo → Конфигурация` добавлен встроенный diff для `active / draft / baseline / editor`
- сравнение можно переключать по сторонам, менять местами и фильтровать до режима «только изменения»
- сохранён safe managed config flow из v1.2.75 без изменения версии `router-agent`
- статус `Router → Agent` по-прежнему перепроверяется автоматически после hotfix из v1.2.77

Текущие версии:
- UI: v1.2.80
- router-agent: 0.6.20

Что важно по интерфейсу:
- `Mihomo` остаётся отдельным рабочим разделом
- `Settings` держит только интерфейс, поведение и связанные настройки отображения
- внутренняя навигация `Mihomo` осталась прежней по логике, менялись только русские подписи и формулировки

Следующий логичный шаг:
- после diff логично показать более подробные причины validate/apply/rollback и вынести их в отдельный читаемый блок диагностики конфигурации
