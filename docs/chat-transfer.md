# UI Mihomo / Ultra — transfer note for next chat

Дата: **2026-04-21**
Текущая версия UI: **v1.2.165**
Текущая версия router-agent: **0.6.32**
Репозиторий: `NightShadowDevOPs/UltraUIXkeen`
Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
Путь на роутере: `/opt/UltraUIXkeen`

## Что вошло в v1.2.165
- Это следующий safe-патч после `v1.2.164`, но уже не про wake-up dedupe, а про аккуратный upstream cherry-pick.
- В `Прокси` добавлена защита для `proxiesRef`, чтобы ранний lifecycle / пустой ref не ломал scroll restore и scroll handler.
- В `Соединения` добавлен явный empty-state, чтобы пустая таблица не выглядела как сломанный экран.
- `router-agent`, HA/export shape, SSL-проверки провайдеров, polling cadence и реальный traffic path не менялись.

## Что важно помнить дальше
- нельзя ломать автоматическую проверку SSL-сертификатов провайдеров
- обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`
- если меняется router-agent, нужно синхронизировать версию в `router-agent/install.sh`, status API, docs и HA handoff bundle
- любые следующие доработки operational/UI-контуров делать осторожно: сначала runtime-безопасность, потом украшательства
- отдельно не ломать структуру данных от router-agent в Home Assistant через agent/handoff

## Что проверить после обновления
1. `Прокси`: страница не падает и не ведёт себя криво, если scroll-state восстанавливается очень рано.
2. `Соединения`: при отсутствии строк виден нормальный empty-state.
3. `Обзор`: диаграмма весов трафика остаётся живой и работает как раньше.
4. HA/export runtime и router-agent snapshot ведут себя как раньше.
5. Реальный трафик через роутер и QoS runtime не деградировали после обновления.

## Следующий логичный шаг
- после живой проверки `v1.2.165` можно продолжать safe upstream review и брать только те изменения, которые дают понятную пользу без роста нагрузки и без риска для runtime
- пока не тащить в релизы ничего, что добавляет новые polling-хвосты или шевелит router-agent без явной необходимости
