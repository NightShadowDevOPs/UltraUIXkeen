# UI Mihomo / Ultra — перенос в новый чат

## Текущее состояние
- Текущая версия UI: **v1.2.131**
- Router-agent: **0.6.26**
- Основной репозиторий: `NightShadowDevOPs/UltraUIXkeen`
- Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
- Путь на роутере: `/opt/UltraUIXkeen`

## Что сделано в v1.2.131
- исправлена логика подписи версии агента: если upstream-версия старее локального агента, UI больше не показывает сбивающее с толку `latest 0.5.57`
- `Router → Сеть` оформлен как отдельная рабочая зона: сверху теперь есть короткая сводка по IP/ping-проверкам и быстрые переходы к `Трафику` и `Провайдерам`
- `router-agent` в этом релизе не менялся и остаётся `0.6.26`

## Что проверять после выкладки
1. `Router → Резервные копии`: у агента при версии `0.6.26` и upstream `0.5.57` показывается нормальная подпись без слова `latest`
2. `Router → Сеть`: открывается новый верхний блок со статусами публичного IP, проверок соединения и целью ping
3. кнопки быстрых переходов из `Router → Сеть` действительно открывают `Трафик` и `Прокси-провайдеров`

## Следующий зафиксированный шаг
- **v1.2.132** — Router traffic workspace polish: дочистить `Router → Трафик`, сократить верхний шум и сделать первый экран ещё полезнее
