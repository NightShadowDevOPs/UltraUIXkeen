UI Mihomo / Ultra — обновление переноса v1.2.81

Что изменилось в этом релизе:
- в `Mihomo → Конфигурация` добавлен диагностический блок последней операции `validate / apply / rollback / restore`
- видно этап, источник, команду проверки, код выхода, способ перезапуска и отдельные выводы первого перезапуска / отката / baseline-восстановления
- для этого `router-agent` переведён на версию `0.6.21`, но сам safe managed-config flow не менялся по логике
- line-diff из v1.2.80 и автоперепроверка статуса `Router → Agent` из v1.2.77 сохранены

Текущие версии:
- UI: v1.2.81
- router-agent: 0.6.21

Что важно по интерфейсу:
- `Mihomo` остаётся отдельным рабочим разделом
- `Settings` держит только интерфейс, поведение и связанные настройки отображения
- внутренняя навигация `Mihomo` осталась прежней по логике, менялись только русские подписи и формулировки

Следующий логичный шаг:
- после v1.2.81 можно возвращаться к следующему функциональному шагу: структурированному обзору популярных секций `config.yaml` или к следующему рабочему блоку проекта
