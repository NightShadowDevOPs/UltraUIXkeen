# UI Mihomo / Ultra — перенос в новый чат

## Текущее состояние
- Текущая версия UI: **v1.2.129**
- Router-agent: **0.6.26**
- Основной репозиторий: `NightShadowDevOPs/UltraUIXkeen`
- Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
- Путь на роутере: `/opt/UltraUIXkeen`

## Что сделано в v1.2.129
- исправлен runtime-сбой в `src/components/router/AgentCard.vue`: inspection panel архива больше не рендерится с переменной `item` вне `v-for`, из-за чего backup workspace мог не появляться вообще
- добавлены template refs для maintenance workspace и первой backup-секции, чтобы `Загрузить` прокручивал к реальному рабочему блоку, а не в пустоту
- `router-agent` в этом релизе не менялся и остаётся `0.6.26`

## Что проверять после выкладки
1. `Router → Резервные копии → Загрузить` теперь реально поднимает backup workspace со списками и действиями, а не оставляет пустой экран
2. `Проверить` раскрывает inspection panel внутри строки архива и не ломает весь блок
3. `Сделать бэкап` и остальные действия в backup workspace работают без регрессий

## Следующий зафиксированный шаг
- **v1.2.130** — Router network workspace hardening: дочистить сетевой рабочий экран и убрать остаточную перегруженность
