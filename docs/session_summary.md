# session_summary — v1.2.187

Prepared UI Mihomo Ultra / router-agent v1.2.187. The release fixes stale CPU in `ha_snapshot.status.system.cpu_pct` by overlaying fresh CPU/load data before snapshot response assembly. Scope is router-agent API only.

## v1.2.190

UI-only provider links layout polish. Provider names are badges, columns are aligned, URL inputs are shorter. Runtime/router-agent logic unchanged.
