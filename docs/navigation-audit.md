# Аудит навигации — v1.2.131

## Что уже разнесено по рабочим зонам
- `Router → Обзор` — короткая живая сводка по состоянию роутера
- `Router → Резервные копии` — отдельный maintenance/workspace для backup create/list/restore/delete/check/schedule
- `Router → Сеть` — отдельная рабочая зона с краткой сводкой по IP/ping-проверкам и быстрыми переходами в соседние сетевые разделы
- `Traffic` — отдельная рабочая зона, внутри которой уже разделены режимы `Devices` и `Users`

## Что сделано в v1.2.131
- исправлена подпись версии агента: если локальный agent новее reported upstream, UI честно показывает `upstream v...` и бейдж `локальная версия новее`, без странного `latest`
- сетевой экран перестал быть хвостом от обзора: добавлена верхняя сводка по публичному IP, connection checks и ping target
- авто-проверку SSL-сертификатов прокси-провайдеров по-прежнему не трогаем

## Что ещё остаётся сделать
1. Довести `Router → Трафик` до такой же логики рабочего места
2. Проверить, не просится ли аналогичная карточная полировка на соседние router-блоки
3. Продолжать аудит верхнего меню и не сваливать operational и maintenance-сценарии в один маршрут
