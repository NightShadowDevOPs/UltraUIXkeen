# Аудит навигации — v1.2.117

## Что изменили в этом шаге

- резервное копирование больше не спрятано внутри `Router → Обзор`
- в `Router` появился отдельный внутренний подраздел `Резервные копии`
- `Обзор` снова выполняет роль короткой сводки, а не смешивает мониторинг и maintenance-сценарии
- добавлен явный переход между `Обзором` и `Резервными копиями`, чтобы backup workflow было видно сразу

---

# Аудит навигации — v1.2.116

## Что изменили в этом шаге

- страница `Трафик` перестала быть одним длинным смешанным экраном
- введены два явных режима работы: `Устройства` и `Пользователи`
- `Устройства` теперь используют живой контур LAN/QoS для быстрой оперативной диагностики
- `Пользователи` оставляют накопленный трафик, лимиты, блокировки и профили как отдельный policy/workflow-экран
- прямые ссылки на `Трафик` теперь можно открывать сразу в нужном режиме через query `view=devices|users`

## Зачем это было нужно

До этого у нас уже были и device/QoS, и user/limits сценарии, но пользователь видел их не как один понятный операционный раздел, а как разнесённые куски. После разделения стало проще: сначала найти шумное устройство, потом уже закрепить правило на уровне пользователя.

---

# Аудит навигации — v1.2.79

## Что изменили

Первый и второй этап разгрузки интерфейса плюс русификация новых элементов:

- отдельный верхнеуровневый раздел `Mihomo`
- редактор конфигурации убран из `Settings`
- боковое меню разбито на логические секции
- `Settings` перегруппирован в более понятные подблоки
- страница `Mihomo` получила внутренние подразделы `Обзор / Состояние / Провайдеры / Правила / Конфигурация`
- быстрые переходы внутри `Mihomo` работают как внутренняя мини-навигация, что особенно полезно на узких экранах

## Текущая структура

### Мониторинг
- Обзор
- Роутер
- Трафик
- Подключения
- Журнал

### Сеть и Mihomo
- Прокси
- Провайдеры прокси
- Подписки
- Правила
- Mihomo

### Управление
- Задачи
- Пользователи
- Политики
- Настройки

## Почему так

До этого `Settings` начинал разрастаться в общий склад всего подряд.
Управление `config.yaml` — это уже не обычная настройка интерфейса, а отдельный операционный контур с черновиком, эталонной версией, историей и откатом, поэтому он вынесен в самостоятельный раздел.

## Что ещё не добито

1. Внутри `Mihomo` пока нет полноценных вложенных роутов: это секции одной страницы, а не отдельные страницы.
2. Нижний мобильный dock всё ещё опирается на общий список маршрутов и может потребовать отдельного упрощённого режима.
3. Нужно решить, выносить ли часть `Tasks` в более предметные разделы, если cockpit users DB и сервисные операции продолжат расти.

## Следующий этап

- решить, нужно ли повышать секции `Mihomo` до настоящих вложенных роутов
- отдельно сделать аудит мобильной навигации
- при необходимости пересобрать `Tasks` и `Settings` ещё на один уровень чище
