# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-20**
Текущая версия UI: **v1.2.157**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.157` router-agent не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся в UI-слое списка задач для панелей прокси-провайдеров.
- Добавлена безопасная зачистка устаревших saved-only записей провайдеров без вмешательства в live traffic/runtime.
- Overview traffic contour, traffic weights chart и связанные HA/export контракты этим релизом не менялись.

## Что проверять после обновления
1. В `Задачи → Прокси-провайдеры → панели управления` строки отключённых провайдеров можно удалить по одной или массово.
2. Для активных провайдеров кнопка `Открыть` и SSL-индикаторы работают как раньше.
3. Overview: диаграмма весов трафика продолжает работать нормально.
4. Router runtime: реальный трафик через роутер не деградирует.

## Следующий мостовой шаг
- после подтверждения `v1.2.157` можно продолжать safe cherry-pick review из upstream и дальше чистить тяжёлые UI-хвосты без роста постоянного polling и без вмешательства в SSL/provider checks
