# UI Mihomo Ultra v1.2.190

Provider access links UI polish release.

The release keeps the v1.2.188/v1.2.189 model: SSL checks use subscription URLs, while public panel URLs and SSH/local panel URLs are separate provider metadata fields. This step only improves the table layout and readability.
