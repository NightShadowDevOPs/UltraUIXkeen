# Rules Digest

- Do not change Mihomo core, TUN, QoS, routing or provider SSL checks unless explicitly requested.
- Do not use 127.0.0.1:9099 for router-agent checks; use 192.168.0.1:9099.
- Keep output compact.
- Release artifacts must use `release-*` naming.
