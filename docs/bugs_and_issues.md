# bugs_and_issues v1.2.192

Fixed visual table column mismatch. Open: fill hosting due dates and SSH panel URLs manually after deployment.


## v1.2.193

- Known risk: UI bundle must be rebuilt/deployed for visible table changes. Source-side checks only confirm markers.
- No known router-agent runtime regression introduced by this UI/store release.
