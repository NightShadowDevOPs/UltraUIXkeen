# UI Mihomo / Ultra — transfer note for next chat

Дата: **2026-04-20**
Текущая версия UI: **v1.2.156**
Текущая версия router-agent: **0.6.32**
Репозиторий: `NightShadowDevOPs/UltraUIXkeen`
Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
Путь на роутере: `/opt/UltraUIXkeen`

## Что вошло в v1.2.156
- UI поднят до `1.2.156`
- router-agent оставлен на `0.6.32`, backend telemetry/runtime path не менялся
- массовые latency-тесты больше не запускают все проверки одним резким параллельным залпом: теперь UI гоняет их с небольшим ограничением по concurrency
- логика выбора test URL стала аккуратнее для single и bulk/manual latency test при включённом режиме independent latency URLs
- основной traffic runtime, SSL-проверки провайдеров и packet-forwarding path специально не трогались

## Что важно помнить дальше
- нельзя ломать автоматическую проверку SSL-сертификатов провайдеров
- обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`
- если меняется router-agent, нужно синхронизировать версию в `router-agent/install.sh`, status API, docs и HA handoff bundle
- любые следующие доработки раздела Трафик делать осторожно: сначала runtime-безопасность, потом украшательства

## Что проверить после обновления
1. Массовый тест задержек по прокси/группе отрабатывает нормально, но роутер не получает резкий всплеск параллельных latency-check запросов.
2. Single latency test и bulk/manual latency test используют ожидаемый test URL, если включён независимый режим URL для проверки.
3. В Обзоре диаграмма весов трафика остаётся живой и работает как раньше.
4. Реальный трафик через роутер и QoS runtime не деградировали после обновления.

## Следующий логичный шаг
- `v1.2.156`: после живой проверки можно продолжать safe upstream review и брать только те изменения, которые снижают burst/load или лишние UI-перерисовки без риска для runtime.
