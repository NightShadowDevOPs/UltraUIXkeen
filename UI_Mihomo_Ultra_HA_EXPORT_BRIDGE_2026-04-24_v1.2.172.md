# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-24**
Текущая версия UI: **v1.2.172**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.172` `router-agent` не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся только в UI-слое страницы `Трафик`.
- Формат данных router-agent для HA, shape JSON и export bundle этим релизом не менялись.
- TUN сознательно не включался и на мост/HA-контур этот релиз не влияет.

## Что проверять после обновления позже
1. `Трафик -> Устройства` и `Трафик -> Пользователи` показывают новые service/empty states.
2. Deep-link и обычные переходы внутри `Трафик` продолжают работать как раньше.
3. Router-agent / HA snapshot продолжает работать как раньше.
4. Provider SSL checks продолжают работать как раньше.
5. Реальный трафик через роутер не деградирует.

## Следующий мостовой шаг
- HA-связку пользователь проверит позже в соседнем проекте; пока в Ultra UI контракт не трогать.
