# UI Mihomo / Ultra — transfer note for next chat

Дата: **2026-04-21**
Текущая версия UI: **v1.2.164**
Текущая версия router-agent: **0.6.32**
Репозиторий: `NightShadowDevOPs/UltraUIXkeen`
Локальная папка: `Y:\Мой диск\Git\UltraUIXkeen`
Путь на роутере: `/opt/UltraUIXkeen`

## Что вошло в v1.2.164
- Это следующий safe-патч после `v1.2.163`.
- Добит ещё один оставшийся wake-up duplicate в `Обзор -> Router Health`.
- Карточка уже делала собственный refresh при повторном входе в viewport, поэтому helper `useSafePolling` больше не автозапускает ещё один такой же wake-up refresh.
- Обычный polling, ручные действия, `router-agent -> HA`, SSL-проверки провайдеров и реальный traffic path не менялись.

## Что важно помнить дальше
- нельзя ломать автоматическую проверку SSL-сертификатов провайдеров
- обновление UI на роутере идёт через встроенный updater интерфейса, а не через `git pull`
- если меняется router-agent, нужно синхронизировать версию в `router-agent/install.sh`, status API, docs и HA handoff bundle
- любые следующие доработки operational/UI-контуров делать осторожно: сначала runtime-безопасность, потом украшательства
- отдельно не ломать структуру данных от router-agent в Home Assistant через agent/handoff

## Что проверить после обновления
1. `Обзор -> Router Health`: при выходе и возврате карточки в viewport не должно быть дубля wake-up refresh.
2. `Router -> System`, `Router -> Router agent`, `Router -> Host QoS`, `Трафик / Пользователи`: логика `v1.2.163` остаётся рабочей, без регрессии.
3. `Задачи`: логика `v1.2.162` остаётся рабочей, без регрессии.
4. `Обзор`: диаграмма весов трафика остаётся живой и работает как раньше.
5. HA/export runtime и router-agent snapshot ведут себя как раньше.
6. Реальный трафик через роутер и QoS runtime не деградировали после обновления.

## Следующий логичный шаг
- после живой проверки `v1.2.164` можно продолжать safe upstream review и брать только те изменения, которые снижают burst/load или лишние wake-up перерисовки без риска для runtime
- если этот хвост подтверждается как закрытый, дальше стоит переключиться не на новые poller-эксперименты, а на аккуратный обзор того, что реально полезно тащить из upstream без роста нагрузки
