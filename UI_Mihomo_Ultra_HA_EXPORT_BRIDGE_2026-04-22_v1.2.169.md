# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-22**
Текущая версия UI: **v1.2.169**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.169` `router-agent` не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся только в UI-слое страницы `Трафик`.
- Формат данных router-agent для HA, shape JSON и export bundle этим релизом не менялись.
- TUN сознательно не включался и на мост/HA-контур этот релиз не влияет.

## Что проверять после обновления
1. В `Трафик` виден только один верхний рабочий переключатель режима.
2. Deep-link переходы по `Пользователю` / `IP` показывают явный focus banner и сбрасываются одной кнопкой.
3. Router-agent / HA snapshot продолжает работать как раньше.
4. Provider SSL checks продолжают работать как раньше.
5. Реальный трафик через роутер не деградирует.

## Следующий мостовой шаг
- после подтверждения `v1.2.169` можно продолжать safe review только для низкорисковых UI/operational-хвостов без роста постоянного polling и без вмешательства в HA/export контракт
