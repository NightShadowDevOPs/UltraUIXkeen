# HA export bridge — UI Mihomo / Ultra

Дата: **2026-04-21**
Текущая версия UI: **v1.2.168**
Текущая версия router-agent: **0.6.32**

## Что важно для HA / внешних handoff-пакетов
- В `v1.2.168` `router-agent` не менялся: HA/export контракт и backend telemetry path оставлены без изменений.
- Изменения релиза находятся только в UI-слое вокруг wake-up refresh для `Router -> System`.
- Формат данных router-agent для HA, shape JSON и export bundle этим релизом не менялись.
- TUN сознательно не включался и на мост/HА-контур этот релиз не влияет.

## Что проверять после обновления
1. Быстрый возврат на вкладку не создаёт дублирующийся wake-up refresh в `Router -> System`.
2. Ручное обновление и загрузка деталей в системной карточке работают штатно.
3. Router-agent / HA snapshot продолжает работать как раньше.
4. Overview: диаграмма весов трафика продолжает работать нормально, а реальный трафик через роутер не деградирует.
5. SSL-проверки провайдеров продолжают работать как раньше.

## Следующий мостовой шаг
- после подтверждения `v1.2.168` можно продолжать safe review только для низкорисковых operational/UI-хвостов без роста постоянного polling и без вмешательства в HA/export контракт
