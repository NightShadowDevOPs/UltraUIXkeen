# Changelog

## v1.2.127 — backup workspace build hotfix

- fixed duplicate identifier in `src/components/router/AgentCard.vue` that broke production build for v1.2.126
- kept the backup workspace UI polish from the previous step
- shifted the next planned router network workspace hardening step to the following release
